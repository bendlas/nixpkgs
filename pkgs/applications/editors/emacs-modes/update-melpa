#! /usr/bin/env nix-shell
#! nix-shell --show-trace ./updater-emacs.nix -i bash

SCRIPT_DIR="$( cd "$(dirname "$0")" ; pwd -P )"

## uncomment to edit script in emacs with updater runtime libs
# exec emacs $SCRIPT_DIR/update-melpa.el

exec emacs --batch --quick -l $SCRIPT_DIR/update-melpa.el -f run-updater "$@"
